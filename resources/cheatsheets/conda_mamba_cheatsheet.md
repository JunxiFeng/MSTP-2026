# Conda/Mamba Cheatsheet

**REFERENCE**

```bash
mamba env create -f environments/day1.yml
conda activate mstp-day1
conda env list
which python
python --version
```

If Mamba is unavailable, use `conda env create -f environments/day1.yml`.
